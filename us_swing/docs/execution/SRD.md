# Software Requirement Document — Execution & Risk Management (EXE)

**Document ID:** SRD-EXE
**Version:** 1.19.0
**Traces To:** FO-EXE v1.12.0
**Status:** Draft
**Last Updated:** 2026-06-12
**Project:** US Swing Trading System

> v1.7.0: Sections 11 + 12 added — SRD-EXE-011.001–015 (Strategy Engine) and SRD-EXE-012.001–013 (Trade Cycle Ledger).
> v1.8.0: SRD-EXE-011.016–019 added — per-symbol re-execution counter (rex_count enforcement).
> v1.9.0: rex_count enforcement implemented and verified.
> v1.10.0: SRD-EXE-011.020 added — `TradeSignal.user_id` propagation from active user via engine `user_id_provider`.
> v1.11.0: Section 13 added — SRD-EXE-013.001–.008 (Strategy Run Lifecycle). SRD-EXE-011.001 and SRD-EXE-011.007 marked Reopen.
> v1.12.0: SRD-EXE-013.001–.008 + SRD-EXE-011.001 + SRD-EXE-011.007 marked Implemented (Final_Execution.md Phase 1).
> v1.13.0: SRD-EXE-012.010/.011 marked Implemented after Phase 2 — `CycleSnapshot.state` typed as `ExecutionEnums.TradeCycleState`; `TradeCycleState.is_terminal()`/`is_non_terminal()` helpers added; `CYCLE_STATES`/`NON_TERMINAL_STATES`/`TERMINAL_STATES` frozensets removed.
> v1.14.0: Section 14 added — SRD-EXE-014.001–.008 (Broker Order State Machine). SRD-EXE-005.001/.002/.003 marked Reopen — `PositionState` removed; `positions.state` column dropped; open/closed derived from `quantity > 0`.
> v1.15.0: Section 15 added — SRD-EXE-015.001–.006 (Broker-Agnostic Order Ingestion & Adapter; Broker_fix.md Phases 2/5/6).
> v1.16.0: Section 16 added — SRD-EXE-016.001–.006 (Retire `positions` table; OrderIngestion-driven monitoring lifecycle; supersedes cleanup.md Steps 7B–8).
> v1.17.0: Section 17 added — SRD-EXE-017.001–.010 (Absolute capital budget, capital-max sizing, advisory risk warnings, rex auto-reset; FO-EXE-017). Refines SRD-EXE-001.002, SRD-EXE-005.004, SRD-EXE-011.011/.017 — see Section 17 notes.
> v1.17.1: SRD-EXE-012.007 corrected (ISS-EXE-0005) — realized PnL multiplies by held `entry_qty`, not the sell fill's reported `exit_qty`, which over-/under-counted shares when the two diverged.
> v1.18.0: SRD-EXE-011.022 added (ISS-EXE-0006) — forced EXIT signals carry the open cycle's last price so paper fills do not record a $0 exit.
> v1.19.0: SRD-EXE-017.015–.021 added (Draft) — global Margin Available ceiling across all strategies, in-flight reservation, per-entry margin clamp, paper open-position-value fix, live margin-drift reconcile, and Max Capital / Margin Available in the User View.

---

## Compact Format Key

| Column | Meaning |
|---|---|
| P | Priority: **Must** / Should / Could |
| Status | Draft / Approved / Implemented / Verified / Reopen |

---

## Section 1: Requirements for FO-EXE-001 — Risk-Controlled Order Submission

| ID | Parent | P | Description | In | Out | Constraints | Status |
|---|---|---|---|---|---|---|---|
| SRD-EXE-001.001 | FO-EXE-001 | Must | `RiskManager.validate_signal(signal, account_state)` checks: (1) max position size, (2) max capital allocation, (3) circuit breaker state. Returns `ValidationResult(ok: bool, reason: str)`. | `TradeSignal`, `AccountState` | `ValidationResult` | Must be synchronous; must not call IBKR API | Implemented |
| SRD-EXE-001.002 | FO-EXE-001 | Must | `RiskManager.calculate_position_size(signal, account_state)` computes share count = `floor((account_equity × risk_per_trade_pct / 100) / abs(entry_price − stop_loss))`. Cap result at `max_position_value / entry_price`. | `TradeSignal`, `AccountState` | `int` shares | Fractional shares are not permitted; always `floor()` | Implemented |
| SRD-EXE-001.003 | FO-EXE-001 | Must | `ExecutionEngine.submit_order(signal, quantity, order_type)` builds an IBKR `Order` object (MKT or LMT), submits via `IBKRClient.place_order()`, and receives an IBKR order ID. | `TradeSignal`, quantity int, order_type | IBKR order ID; order persisted to `trades` table | Submission must complete within 2 s; timeout raises `OrderSubmissionError` | Implemented |
| SRD-EXE-001.004 | FO-EXE-001 | Must | Immediately on `submit_order()` success, write a `TradeRecord(trade_id=ibkr_order_id, user_id, symbol, entry_time, entry_price=signal.entry_price, quantity, mode, status='SUBMITTED')` to the `trades` table. | IBKR order ID, `TradeSignal`, `user_id`, `mode` | row in `trades` table | Must insert before returning the order ID to the caller | Implemented |
| SRD-EXE-001.005 | FO-EXE-001 | Must | Rejected signals (failed validation) must be logged at WARNING: `Signal REJECTED for {symbol}: {reason}`. No order submitted. | `ValidationResult(ok=False)` | WARNING log | Rejection must not raise an exception; caller receives `None` order ID | Implemented |
| SRD-EXE-001.006 | FO-EXE-001 | Should | When `order_type = 'LMT'`, limit price = `signal.entry_price`. Accept a configurable `slippage_ticks` offset (default 0) added to limit price for BUY orders to improve fill probability. | config `slippage_ticks`, tick size | adjusted limit price | Tick size obtained from IBKR contract details at subscription time | Implemented |

---

## Section 2: Requirements for FO-EXE-002 — Position Tracking & Exit Execution

| ID | Parent | P | Description | In | Out | Constraints | Status |
|---|---|---|---|---|---|---|---|
| SRD-EXE-002.001 | FO-EXE-002 | Must | `PositionTracker` maintains an in-memory `dict[tuple[int,str], OpenPosition]` keyed by `(user_id, symbol)`, and mirrors state to the `positions` table in the database. On every state change, DB update is performed asynchronously. | position events | in-memory + DB state synchronised | Must be thread-safe; concurrent reads from `StrategyEngine` must not corrupt state | Implemented |
| SRD-EXE-002.002 | FO-EXE-002 | Must | `ExecutionEngine.handle_order_fill(fill_event)` receives IBKR fill callback; if fill is for an **entry** order: creates `OpenPosition` and registers it in `PositionTracker`. If fill is for an **exit** order: removes `OpenPosition`, updates `trades` table with `exit_time`, `exit_price`, `pnl`. | IBKR `Fill` event | updated `PositionTracker`; updated `trades` row | `pnl = (exit_price − entry_price) × quantity` for long; must handle partial fills by updating quantity | Implemented |
| SRD-EXE-002.003 | FO-EXE-002 | Must | On receiving a SELL signal from `StrategyEngine`, `ExecutionEngine.exit_position(symbol)` looks up the full open quantity from `PositionTracker` and submits a market SELL order for the exact quantity. | SELL `TradeSignal`, `PositionTracker` | IBKR SELL order submitted | Exit order must be submitted within 1 s of receiving the signal | Implemented |
| SRD-EXE-002.004 | FO-EXE-002 | Must | On application startup, `PositionTracker.reconcile(ibkr_positions)` compares IBKR-reported open positions against the local `positions` table. Any discrepancy (position in IBKR not in DB) is adopted into `PositionTracker` and logged at WARNING. | IBKR account positions list | reconciled `PositionTracker` state | Must run before `StrategyEngine` begins evaluating signals | Implemented |
| SRD-EXE-002.005 | FO-EXE-002 | Must | On position close, emit a `PositionClosedEvent(symbol, pnl, strategy_id, duration)` to all subscribers (Logger, DailyPnLTracker). | filled exit order | `PositionClosedEvent` dispatched | Event dispatch must be non-blocking | Implemented |

---

## Section 3: Requirements for FO-EXE-003 — Daily Loss Circuit Breaker & Emergency Controls

| ID | Parent | P | Description | In | Out | Constraints | Status |
|---|---|---|---|---|---|---|---|
| SRD-EXE-003.001 | FO-EXE-003 | Must | `DailyPnLTracker` accumulates realised PnL from all `PositionClosedEvent` emissions. Exposes `daily_pnl: float` property. Resets to 0 at market open each day. | `PositionClosedEvent` | running `daily_pnl` float | Must be thread-safe; UI or CLI may read at any time | Draft |
| SRD-EXE-003.002 | FO-EXE-003 | Must | `CircuitBreaker.check(daily_pnl, account_equity)` returns True (breach) when `daily_pnl ≤ −(account_equity × max_daily_loss_pct / 100)`. Called after every `PositionClosedEvent`. | `daily_pnl`, `account_equity`, `max_daily_loss_pct` | `bool` breach flag | Must use start-of-day equity snapshot (taken at startup), not live equity to avoid feedback loops | Draft |
| SRD-EXE-003.003 | FO-EXE-003 | Must | `EmergencyShutdown.run(reason)` executes: (1) cancel all pending IBKR orders, (2) submit market close orders for all open positions, (3) set `circuit_breaker_active = True` flag, (4) log CRITICAL with reason, (5) send alert, (6) stop `LiveEngine` event loop. | trigger reason string | complete halt; all positions closed | Must complete within 60 s; IBKR errors during shutdown are logged but not re-raised | Draft |
| SRD-EXE-003.004 | FO-EXE-003 | Must | While `circuit_breaker_active = True`, any entry signal arriving at `ExecutionEngine.submit_order()` is silently discarded and logged at DEBUG: `Signal discarded: circuit breaker active`. | `TradeSignal`, `circuit_breaker_active` flag | signal discarded + DEBUG log | Must also block new exit submissions that are not part of the emergency shutdown sequence | Draft |
| SRD-EXE-003.005 | FO-EXE-003 | Must | Manual kill-switch: `EmergencyShutdown.run(reason='manual')` must be callable via: (a) CLI `python -m us_swing kill`, (b) a SIGTERM signal handler, (c) GUI emergency button. All routes must call the same `EmergencyShutdown.run()` implementation. | CLI command, SIGTERM, or GUI button | `EmergencyShutdown.run('manual')` executed | SIGTERM handler must be registered on `main.py` startup | Draft |
| SRD-EXE-003.006 | FO-EXE-003 | Should | After `EmergencyShutdown.run()` completes, write a shutdown summary to `logs/shutdown_YYYYMMDD_HHMMSS.log`: date, trigger reason, positions closed, final daily PnL, any IBKR errors encountered during shutdown. | shutdown state | shutdown summary log file | File must be machine-readable JSON for automated post-mortem analysis | Draft |

---

## Section 4: Requirements for FO-EXE-004 — Paper Trading Mode

| ID | Parent | P | Description | In | Out | Constraints | Status |
|---|---|---|---|---|---|---|---|
| SRD-EXE-004.001 | FO-EXE-004 | Must | `PaperEngine` class implements the same interface as the live `ExecutionEngine` for order submission but simulates fills without calling IBKR API. Market orders fill immediately at current market price; limit orders fill when price crosses limit level. | `TradeSignal`, quantity, order_type | simulated fill event | Must produce identical `TradeRecord` and `OpenPosition` structures as live engine | Implemented |
| SRD-EXE-004.002 | FO-EXE-004 | Must | `PaperEngine` writes all trades to the `trades` table with `mode = 'paper'`. Position updates go to `positions` table with `mode = 'paper'`. | simulated fill | DB rows with `mode='paper'` | Paper and live records are in the same tables; differentiated solely by `mode` column | Implemented |
| SRD-EXE-004.003 | FO-EXE-004 | Must | `PaperEngine` calculates P&L identically to live: `pnl = (exit_price − entry_price) × quantity` for long positions. | simulated exit fill | `pnl` value | Rounding behaviour must match live P&L calculation exactly | Implemented |
| SRD-EXE-004.004 | FO-EXE-004 | Must | `PaperEngine` uses live market data (from `DataProvider`) for fill price reference. No synthetic price generation for paper fills. | current market price from live feed | fill at market price | If no live feed available (e.g., after hours), paper fills are queued until next live price | Implemented |
| SRD-EXE-004.005 | FO-EXE-004 | Should | `ExecutionRouter` selects `PaperEngine` or live `ExecutionEngine` based on the active user's `mode` setting. Routing is determined at signal submission time. | `user.mode`, `TradeSignal` | routed to correct engine | Mode check must be per-signal, not cached at startup; user can switch mid-session | Implemented |

---

## Section 5: Requirements for FO-EXE-005 — Position State Machine & Capital Check

| ID | Parent | P | Description | In | Out | Constraints | Status |
|---|---|---|---|---|---|---|---|
| SRD-EXE-005.001 | FO-EXE-005 | Must | Position open/closed status is derived: `open ⇔ positions.quantity > 0`. The `PositionState` enum and the `positions.state` column are removed; broker-order progress lives on `trades.order_state` per FO-EXE-014. | fill events | derived `open` flag | No separate state column; `PositionTracker.has_open(user_id, symbol)` returns `quantity > 0` | Reopen |
| SRD-EXE-005.002 | FO-EXE-005 | Must | On partial entry fill: increment `positions.quantity` by the fill quantity. The owning `trades` row is updated to `order_state='PARTIAL_FILLED'` and `filled_quantity` is bumped per FO-EXE-014. | partial fill event | updated position | Cumulative; multiple partial fills sum into `positions.quantity` | Reopen |
| SRD-EXE-005.003 | FO-EXE-005 | Must | On partial exit fill: decrement `positions.quantity` by the fill quantity. The owning SELL `trades` row is updated to `order_state='PARTIAL_FILLED'` and `filled_quantity` is bumped; full exit leaves `positions.quantity = 0`. | exit fill event | updated position | Cycle realized PnL persists in `trade_cycles.realized_pnl_usd`, not on `trades` | Reopen |
| SRD-EXE-005.004 | FO-EXE-005 | Must | `RiskManager.can_enter_new(signal, account_state, user_id)` checks: `available_capital = total_equity - sum(open_position_values_for_user)`. Returns True only if the projected position fits within `max_allocation_pct`. | `TradeSignal`, `AccountState`, `user_id` | `bool` | Capital calculation is per-user; each user has independent allocation limits | Implemented |
| SRD-EXE-005.005 | FO-EXE-005 | Must | User quantity override: `ExecutionEngine.submit_signal()` accepts an optional `quantity_override: int | None` parameter. If provided, this quantity is used instead of `RiskManager.calculate_position_size()`. Override must still pass capital availability check. | `quantity_override` int, `TradeSignal` | order with overridden quantity | Override quantity must be > 0; override does not bypass risk validation, only position sizing | Implemented |
| SRD-EXE-005.006 | FO-EXE-005 | Must | Positions with state != CLOSED persist across application restarts. On startup, `PositionTracker.load_from_db(user_id)` restores all non-CLOSED positions. | DB `positions` rows | restored in-memory `PositionTracker` | Must run before `StrategyEngine` begins evaluating signals; reconcile with IBKR positions after loading from DB | Implemented |

---

## Section 6: Requirements for FO-EXE-006 — Intraday Candle Readiness (Phase 1: Download)

| ID | Parent | P | Description | In | Out | Constraints | Status |
|---|---|---|---|---|---|---|---|
| SRD-EXE-006.001 | FO-EXE-006 | Must | `IntradayCandleLoader.load(symbols: list[str])` is the single entry point triggered when a screened stock list is available. It orchestrates delta-fetch of 1 m bars for every symbol and persists them via `DatabaseManager.insert_bars()`. Runs in a background thread; emits `load_progress(symbol, done, total)` and `load_complete(results: list[CandleLoadResult])` signals upon completion. `CandleLoadResult` carries `symbol: str`, `ok: bool`, `reason: str` — richer than a bare failed-symbol list and required for SRD-EXE-006.009 readiness mapping. | `list[str]` symbols, `IBKRClient`, `DatabaseManager` | 1 m bars in `price_1m`; progress + completion signals | Must not block the calling thread; per-symbol failures must not abort other symbols | Implemented |
| SRD-EXE-006.002 | FO-EXE-006 | Must | **Delta fetch logic:** `IntradayCandleLoader._fetch_symbol(symbol)` calls `DatabaseManager.get_last_timestamp(symbol, '1m')` to find the last stored bar. If result is None, fetch the full historical window. If a timestamp exists, fetch only bars after that timestamp. Uses `IBKRClient.req_historical_data()` via the pacing queue (SRD-INF-001.005). | symbol, last stored timestamp | new 1 m bars (0 rows if already current); no duplicate inserts | Duplicate prevention: `DatabaseManager.insert_bars()` uses INSERT OR IGNORE; method is idempotent | Implemented |
| SRD-EXE-006.003 | FO-EXE-006 | Must | **Minimum candle window:** The historical fetch window must be large enough to produce ≥ 390 aggregated candles for each required timeframe: 3 m (requires 1 170 minutes of 1 m bars ≈ 3 trading days), 15 m (5 850 min ≈ 15 trading days). On first fetch (no prior data), the loader requests **30 calendar days** (≈ 21 trading days) of 1 m bars per symbol to satisfy both timeframes with a 6-day safety margin. | symbol, target timeframes | sufficient 1 m bar rows to aggregate 390+ candles for 3 m / 15 m | IBKR 1 m bar limit: maximum 30 calendar days per request; 30 calendar days fits in a single page — no paging required for fresh fetch | Implemented |
| SRD-EXE-006.004 | FO-EXE-006 | Must | **Post-fetch validation:** After inserting bars for a symbol, `IntradayCandleLoader._validate_candle_counts(symbol)` calls `HistoricalDataEngine.aggregate_timeframe(symbol, tf)` for tf ∈ {`'3m'`, `'15m'`} and checks that each returns ≥ 390 bars. If any timeframe is short, the symbol is added to `load_complete.failed` list with reason `'insufficient_candles:{tf}:{count}'`. | symbol, `DatabaseManager`, `HistoricalDataEngine` | pass/fail per timeframe; short symbols in failed list | Validation is read-only; does not re-fetch; caller decides whether to retry | Implemented |
| SRD-EXE-006.005 | FO-EXE-006 | Must | **Per-symbol error isolation:** Any exception raised during `_fetch_symbol()` or `_validate_candle_counts()` (IBKR timeout, pacing error, DB write failure) is caught, a `CandleLoadResult(symbol, ok=False, reason=repr(exc))` is appended to the results list, and processing continues with the next symbol. After all symbols, the full results list (successes and failures) is emitted via `load_complete` and failures are logged at WARNING. | exception from IBKR or DB | `CandleLoadResult(ok=False)` in results; WARNING log entry | Must not re-raise; failed symbols are eligible for manual retry via `load([failed_symbol])` | Implemented |
| SRD-EXE-006.006 | FO-EXE-006 | Should | `IntradayCandleLoader.get_readiness_report(symbols: list[str]) -> dict[str, dict]` returns a per-symbol dict with keys: `candles_3m`, `candles_15m` (int counts), `ready` (bool — True if both ≥ 390), `last_1m_bar` (datetime or None). Used by the GUI to display candle readiness before market open. | `list[str]` symbols, `DatabaseManager`, `HistoricalDataEngine` | readiness dict | Must complete in < 2 s for ≤ 500 symbols; uses cached count queries, not full bar fetches | Implemented |
| SRD-EXE-006.007 | FO-EXE-006 | Must | **Auto-trigger wiring — `AppService._on_screener_results_updated(entries)`:** `AppService.__init__` connects `self.screener_results_updated` to a private slot `_on_screener_results_updated(entries: list[FilteredStockEntry])`. The slot: (1) extracts a deduplicated, sorted `list[str]` of symbols from the entries; (2) if the list is empty, returns immediately; (3) reads IBKR connection params (`host`, `port`, `client_id`) from `SystemConfig`; (4) if `SystemConfig.ibkr_enabled` is False, logs INFO `"Candle auto-fetch skipped — IBKR disabled"` and returns; (5) otherwise instantiates `IntradayCandleLoader` with the symbol list and config, connects its `load_progress` to `AppService.candle_load_progress` and `load_complete` to `AppService._on_candle_load_complete`, stores the instance in `self._intraday_loader`, and calls `self._intraday_loader.start()`. | `list[FilteredStockEntry]` from signal | `IntradayCandleLoader` started in background; `INFO` log entry | `client_id` for the intraday loader must be distinct from the main IBKR `client_id` to avoid connection conflicts; use `SystemConfig.ibkr_intraday_client_id` | Implemented |
| SRD-EXE-006.008 | FO-EXE-006 | Must | **Already-running guard:** Before starting a new `IntradayCandleLoader` (step 5 of SRD-EXE-006.007), check `self._intraday_loader is not None and self._intraday_loader.isRunning()`. If True: store the incoming symbol list as `self._pending_candle_symbols: list[str] \| None` and return without starting a second loader. In `_on_candle_load_complete`, after processing results: if `_pending_candle_symbols` is not None, immediately start a new loader for those symbols (clearing `_pending_candle_symbols` to None first). This ensures the latest screener results are always processed without overlapping IBKR connections. Log at WARNING when a run is deferred: `"IntradayCandleLoader already running — deferring {n} symbols"`. | running `_intraday_loader`, incoming symbol list | at most one active IBKR candle-loader connection at any time; deferred symbols always processed | Deferred list replaces, not appends to, any prior deferred list — only the latest screener result matters | Implemented |
| SRD-EXE-006.009 | FO-EXE-006 | Should | **GUI candle readiness column in `_FilteredStocksPane`:** Add a `Candles` column (index 5) to `_FilteredStocksModel`. Values: `"✓"` (green, `C.GREEN`) if `candle_ready is True`; `"⟳"` (yellow, `C.YELLOW`) if loading in progress; `"—"` (muted) if unknown. `AppService` emits a new `candle_readiness_updated = pyqtSignal(dict)` signal (payload: `dict[str, bool]` mapping symbol → ready) from `_on_candle_load_complete`. `_FilteredStocksPane` connects to this signal and calls a `set_candle_readiness(report: dict[str, bool])` method on the model which updates an internal `_readiness: dict[str, bool]` map and calls `self.dataChanged`. While `_intraday_loader.isRunning()` `AppService` emits `candle_readiness_updated` with the in-progress symbol set mapped to a sentinel value so the pane can show `"⟳"` for those symbols. | `candle_readiness_updated` signal, `dict[str, bool]` | per-row readiness badge in `_FilteredStocksPane` table | Column header: `"Candles"`; fixed width 58 px; `ResizeMode.Fixed`; no sorting on this column | Approved |
| SRD-EXE-006.010 | FO-EXE-006 | Must | The execution candle provider builds each strategy-evaluation timeframe (3m, 15m) by aggregating stored 1m bars via `HistoricalDataEngine.aggregate_timeframe` and merging any live `price_{tf}` bars, giving strategies full historical depth without an active live session. | symbol; `price_1m` + live `price_{tf}` rows in candles.db | `dict[str, pd.DataFrame]` of merged bars per timeframe plus a latest-bar accessor | 1m is the source of truth; live bars win on timestamp conflict; timeframe omitted when neither source has bars | Implemented |
| SRD-EXE-006.012 | FO-EXE-006 | Must | The execution candle provider also includes stored daily (1d) and weekly (1w) closed bars, read directly from `price_1d` / `price_1w` in candles.db, so both entry and exit conditions can reference daily/weekly timeframes. Strategy Builder Timeframe dropdowns offer `1d` and `1w`. | symbol; `price_1d` / `price_1w` rows in candles.db | `1d` / `1w` frames added to the `dict[str, pd.DataFrame]` | Closed bars only — no aggregation, no live merge, no synthetic current-day bar; timeframe omitted when no stored bars exist | Implemented |

---

## Section 7: Requirements for FO-EXE-007 — Live 3m Candle Formation (Phase 2: Live Feed)

| ID | Parent | P | Description | In | Out | Constraints | Status |
|---|---|---|---|---|---|---|---|
| SRD-EXE-007.001 | FO-EXE-007 | Must | **Schema extension — `price_3m` table:** Add a `price_3m` SQLAlchemy table to `db/schema.py` with the same column layout as `price_1m` (`symbol TEXT`, `datetime TEXT` ISO 8601 UTC, `open FLOAT`, `high FLOAT`, `low FLOAT`, `close FLOAT`, `volume INTEGER`; PRIMARY KEY `(symbol, datetime)`). Add a compound index `idx_price_3m_sym_dt`. Register `"3m": price_3m` in `PRICE_TABLES` so that all existing `DatabaseManager` methods (`insert_bars`, `get_last_timestamp`, `get_bars`) work for timeframe `'3m'` without modification. Apply via `create_schema(engine, checkfirst=True)` — additive, no migration needed for existing databases. | `schema.py`, `create_schema()` | new `price_3m` table + index + `PRICE_TABLES["3m"]` entry | Must not alter existing tables; `checkfirst=True` ensures no error on re-run | Approved |
| SRD-EXE-007.002 | FO-EXE-007 | Must | **`CandleBuilder` window state:** Candle accumulation is handled entirely by `CandleBuilder` (in `analysis/candle_builder.py`). Internal state per (symbol, timeframe): `_buffers[symbol][tf]: list[RealtimeBar]` (raw trade ticks accumulated since window open), `_window_start[symbol][tf]: datetime \| None` (UTC start of the current window, floor-aligned to the timeframe boundary via Unix-timestamp flooring). `tick_count` is implicit in `len(_buffers[symbol][tf])`. Candle output is produced by `_aggregate(symbol, tf, buf, window_start) -> OHLCVBar`; the candle's `datetime` is `window_start`, not the first tick's timestamp. | — | In-memory window state per (symbol, tf); `OHLCVBar` emitted on window close | `window_start` is Unix-timestamp floor-aligned (e.g. 3m boundary = every 3 minutes from epoch); never emit a candle with an empty buffer | Implemented |
| SRD-EXE-007.003 | FO-EXE-007 | Must | **`LiveBarWorker(QThread)` class and signals:** Implement `LiveBarWorker` in `execution/live_bar_worker.py`. Constructor accepts `symbols: list[str]`, `ibkr_host: str`, `ibkr_port: int`, `ibkr_client_id: int`, `db_path: str`. Candle aggregation is delegated to `CandleBuilder` imported via `core/candle_builder.py` (re-exported from `analysis/candle_builder.py`). Declare one PyQt signal: `candle_closed = pyqtSignal(str)` (emitted when any configured timeframe bar finalises and is written to DB; carries `symbol`). The thread's `run()` launches an asyncio event loop via `asyncio.run(_async_run())`, subscribes to IBKR individual trade ticks via `ib.reqTickByTick(contract, 'Last', numberOfTicks=0, ignoreSize=False)` for each symbol, and routes each tick through `CandleBuilder.add_bar()` which closes candles on time-window boundaries and writes to `price_3m` / `price_15m`. Falls back to yfinance polling (60s interval) if IBKR is unavailable. The thread must be startable and stoppable without restarting the process. | `symbols`, IBKR connection params, `db_path` | `LiveBarWorker` instance; `candle_closed` signal | Signal connections are made by `AppService`; `LiveBarWorker` must not import GUI modules; `reqTickByTick` does not support `useRTH` — RTH filtering is performed per-tick via `_is_rth()` | Implemented |
| SRD-EXE-007.004 | FO-EXE-007 | Must | **`set_symbols(symbols: list[str])` — dynamic subscription management:** `set_symbols()` diffs the new symbol list against the current subscription set (`self._subscribed: set[str]`). For each symbol in `new − current`: call `ib.reqTickByTick(contract, 'Last', 0, False)`, store the returned ticks object in `self._tick_subs[symbol]`, and add to `_subscribed`. For each symbol in `current − new`: call `ib.cancelTickByTick(self._tick_subs.pop(symbol))`, remove from `_subscribed`, and call `builder.reset(symbol)` to clear its in-progress window state. The entire operation is protected by `threading.Lock` because `set_symbols()` may be called from the GUI thread while tick callbacks run in the ib_insync event loop thread. | `list[str]` new symbol list | updated `_subscribed` set; IBKR tick subscriptions added/cancelled; `CandleBuilder` window state cleared for removed symbols | Must not call `reqTickByTick` for symbols already subscribed; no duplicate tick subscriptions | Implemented |
| SRD-EXE-007.005 | FO-EXE-007 | Must | **`_make_tick_handler(symbol, builder)` — trade tick processing:** Returns an ib_insync `updateEvent` closure for *symbol*. Handler steps in order: (1) If `has_new` is False or ticks list is empty: return. (2) Take `tick = ticks[-1]` (newest trade). (3) Parse `tick.time` → UTC `datetime`. (4) Call `_is_rth(dt)` — return early if False (pre/post-market ticks discarded). (5) Read `price = float(tick.price)`, `size = int(tick.size)`; if either ≤ 0, return. (6) Construct `RealtimeBar(symbol, datetime=dt, open=price, high=price, low=price, close=price, volume=size)`. (7) Call `builder.add_bar(bar)`. `CandleBuilder` handles window tracking, time-boundary detection, candle emission, and the `on_candle_closed` callback — no state management needed in the handler itself. | `ticks: TickByTickAllLast`, `has_new: bool` (from ib_insync updateEvent) | `RealtimeBar` routed to `CandleBuilder`; `candle_closed` signal emitted by `_on_candle_closed` callback on window boundary | Handler runs on the ib_insync asyncio loop thread; `CandleBuilder` methods called from this handler need no additional locking since all tick callbacks for the same connection are serialised on that loop | Implemented |
| SRD-EXE-007.006 | FO-EXE-007 | Must | **`_on_candle_closed(symbol, tf, bar)` — candle finalisation callback:** Registered as `CandleBuilder.on_candle_closed`. Called by `CandleBuilder._process_bar` when a time window closes with accumulated ticks. Steps: (1) If `tf` not in `_LIVE_TIMEFRAMES` (`{"3m", "15m"}`): return. (2) Call `_write_bar(symbol, tf, bar)` — synchronous SQLite `INSERT OR IGNORE` into `price_{tf}`; duplicate timestamps silently ignored. (3) Log at DEBUG: `"[Live] {symbol} — {tf} candle closed @ {bar.datetime}"`. (4) Emit `candle_closed(symbol)` Qt signal. DB write is synchronous on the ib_insync callback thread (not the GUI thread); must complete within 500 ms. | `symbol: str`, `tf: str`, `bar: OHLCVBar` | `OHLCVBar` persisted to `price_{tf}`; `candle_closed` signal emitted | Only called from `CandleBuilder` via the registered callback; never called directly | Implemented |
| SRD-EXE-007.007 | FO-EXE-007 | Must | **RTH guard — `_is_rth(dt: datetime) -> bool`:** Module-level function in `live_bar_worker.py`. Returns `True` only if `dt`, converted to US/Eastern time via `zoneinfo.ZoneInfo("America/New_York")`, is on a weekday and falls within `09:30:00 ≤ dt_et < 16:00:00`. Applied per tick in `_make_tick_handler` (step 4). Ticks outside RTH are discarded without reaching `CandleBuilder`. Partial windows open at session end are discarded automatically — `CandleBuilder` never receives an out-of-RTH tick to trigger a window close, so in-progress buffers are simply abandoned; they are cleared when `builder.reset(symbol)` is called on the next session's `set_symbols()` call or on disconnect. No QTimer polling is required. | `datetime` (UTC) | `bool` | Federal holidays are treated as non-RTH days (no market data received, so no action needed); DST transitions handled correctly via `zoneinfo` (not `pytz`) | Implemented |
| SRD-EXE-007.008 | FO-EXE-007 | Must | **Disconnect / reconnect handling:** On IBKR disconnect, `LiveBarWorker` detects the dropped connection via `ib.isConnected()` returning False in the main loop, exits cleanly, and calls `builder.reset(symbol)` for each subscribed symbol — discarding all in-progress windows without persisting. Logs WARNING: `"[Live] IBKR real-time subscription ended"`. On reconnect (i.e., `AppService` calls `_start_live_bar_worker()` again), a fresh `LiveBarWorker` is constructed; fresh windows begin on the first incoming tick for each symbol. `request_stop()` triggers `ib.disconnect()` which tears down all tick subscriptions server-side; no per-symbol `cancelTickByTick` is needed in the stop path. No gap-fill fetch is performed on reconnect — `IntradayCandleLoader` (Phase 1) is responsible for back-filling missing historical bars. | — | in-progress windows discarded on disconnect; fresh worker created on reconnect | `request_stop()` is called by `AppService._stop_live_bar_worker()` before constructing a new worker; partial windows from the previous session are never persisted | Implemented |
| SRD-EXE-007.009 | FO-EXE-007 | Should | **`get_readiness_report` consistency after live close:** After a `candle_closed` event persists a new 3m bar for `symbol` via `insert_bars()`, a subsequent call to `IntradayCandleLoader.get_readiness_report([symbol])` must return `candles_3m` = (prior count + 1). This holds automatically because `get_readiness_report` queries `price_3m` row counts from the database directly (no stale in-memory cache). No additional integration code is required; this requirement exists to specify the observable contract and to drive the integration test that verifies it. | `candle_closed` event → DB insert | `get_readiness_report` reflects updated count | `get_readiness_report` must query `price_3m` (not only the 1m-aggregated view) — requires SRD-EXE-006.006 to be updated so `candles_3m` reads from `price_3m` row count rather than aggregating from `price_1m`; this update is in scope for Phase 2 implementation | Approved |

---

## Section 8: Requirements for FO-EXE-008 — Live Market Data Tick Worker

| ID | Parent | P | Description | In | Out | Constraints | Status |
|---|---|---|---|---|---|---|---|
| SRD-EXE-008.001 | FO-EXE-008 | Must | **`LiveTickWorker(QThread)` class:** Implement `LiveTickWorker` in `execution/live_tick_worker.py`. Constructor signature: `__init__(self, host: str, port: int, client_id: int, parent: QObject \| None = None)`. The worker owns a private `ib_insync.IB()` instance and a private `asyncio` event loop, launched via `asyncio.run(self._async_run())` in `QThread.run()`. Declare two PyQt signals: `tick_price = pyqtSignal(str, float)` (tag, last_price) and `subscription_failed = pyqtSignal(str, int)` (tag, ibkr_error_code). `client_id` is sourced from `SystemConfig.ibkr_tick_client_id` (default 14) by the caller (AppService). | `host`, `port`, `client_id` | `LiveTickWorker` instance; `tick_price` and `subscription_failed` signals | Must not import any GUI module; no DB dependency; must be importable standalone | Approved |
| SRD-EXE-008.002 | FO-EXE-008 | Must | **`set_contracts(contracts: dict[str, Contract]) -> None`:** Public thread-safe method called from the GUI thread. Reconciles the active subscription set by diffing `contracts` against `self._active: dict[str, Contract]`. For each tag in `new − active`: call `ib.reqMktData(contract, genericTickList="", snapshot=False, regulatorySnapshot=False)`, store the returned `Ticker` in `self._tickers[tag]`, add to `_active`. For each tag in `active − new`: call `ib.cancelMktData(self._tickers.pop(tag).contract)`, remove from `_active`. New subscriptions are batched in groups of 10 with a 200 ms sleep between batches. All mutations to `_active` and `_tickers` are protected by `threading.Lock`. | `dict[str, Contract]` new desired set | Updated IBKR subscriptions; `_active` reconciled | Must not call `reqMktData` for already-subscribed tags; idempotent when called with the same set | Approved |
| SRD-EXE-008.003 | FO-EXE-008 | Must | **`tick_price` emission via `pendingTickersEvent`:** In `_async_run()`, register `ib.pendingTickersEvent += self._on_pending_tickers`. `_on_pending_tickers(tickers: set[Ticker])` iterates tickers, looks up `tag = self._tag_by_conid.get(ticker.contract.conId)`, resolves price with fallback chain `ticker.last → ticker.close` (skipping if the resolved value is `NaN` or `≤ 0`), and emits `self.tick_price.emit(tag, price)`. `_tag_by_conid` is populated in `set_contracts()` as `{ticker.contract.conId: tag}` after `reqMktData` returns a Ticker with a known conId. | `ib.pendingTickersEvent` callback, `Ticker` objects | `tick_price(tag, price)` emitted for each Ticker with a valid price | Runs on the ib_insync asyncio thread; `tick_price` emission is thread-safe via Qt's cross-thread signal queuing | Approved |
| SRD-EXE-008.004 | FO-EXE-008 | Must | **IBKR error handling — `subscription_failed`:** Register `ib.errorEvent += self._on_ibkr_error`. `_on_ibkr_error(reqId, code, msg, contract)` checks if `code` is in `{200, 354, 420}` (no contract found, no market data subscription, invalid realtime query). If so: look up `tag` via `self._reqid_to_tag.get(reqId)`; if found, emit `subscription_failed.emit(tag, code)`, call `ib.cancelMktData(contract)`, remove tag from `_active` and `_tickers` and `_tag_by_conid`. All other error codes are logged at WARNING and ignored. `_reqid_to_tag` is populated in `set_contracts()` immediately after `reqMktData` by reading `ticker.reqId`. | `ib.errorEvent` callback, `reqId`, `code`, `contract` | `subscription_failed(tag, code)` emitted; tag removed from active set; other subscriptions unaffected | Error codes to handle: 200 (no security definition), 354 (not subscribed), 420 (invalid query) | Approved |
| SRD-EXE-008.005 | FO-EXE-008 | Must | **`request_stop() -> None`:** Public method called from any thread. Sets a threading event `self._stop_event`. The `_async_run()` loop detects the event and: (1) calls `ib.cancelMktData(ticker.contract)` for all `ticker` in `self._tickers.values()`; (2) calls `await ib.disconnectAsync()`; (3) exits. `QThread.run()` returns, making the thread joinable. The total time from `request_stop()` to `QThread.isFinished()` must be ≤ 3 s under normal conditions. | `_stop_event` set | All reqMktData cancelled; IBKR disconnected; thread exits | Cancelling all subscriptions before disconnect avoids server-side subscription leaks; `disconnectAsync` should complete within 1 s | Approved |
| SRD-EXE-008.006 | FO-EXE-008 | Should | **ClientId collision retry:** If IBKR connect returns error code 326 ("Client id already in use"), `_async_run()` increments `self._client_id` by 1 and retries the `connectAsync()` call, up to 3 additional attempts. Each retry logs `WARNING "[Tick] ClientId {id} in use — retrying with {id+1}"`. After 3 failed retries the worker logs `ERROR "[Tick] Cannot connect — all clientId slots in use"` and exits without emitting any signal. | IBKR error 326 | Retry with incremented clientId; up to 3 retries | Do not use clientIds already assigned to other workers (10, 12, 13); start retry from `ibkr_tick_client_id + 1` | Approved |

---

## Section 9: Requirements for FO-EXE-009 — Intraday Monitoring Session Ledger & Lifecycle

| ID | Parent | P | Description | In | Out | Constraints | Status |
|---|---|---|---|---|---|---|---|
| SRD-EXE-009.001 | FO-EXE-009 | Must | Declare `monitoring_session` table keyed `(session_date, symbol)` with preset id, run timestamp, lifecycle state, and entered/exited/evicted timestamps. | `schema.py` | new table + 2 indexes | Additive via `create_schema(checkfirst=True)`; rows never deleted by app code | Implemented |
| SRD-EXE-009.002 | FO-EXE-009 | Must | Add nullable `trade_origin` (`system`/`manual`) and `monitoring_session_date` columns to `trades`. | DB engine | 2 new columns | Idempotent `ALTER TABLE` migration; legacy NULL rows treated as manual | Implemented |
| SRD-EXE-009.003 | FO-EXE-009 | Must | Add nullable `origin` and `anchor_session_date` columns to `positions`. | DB engine | 2 new columns | Idempotent migration; "system position" = `state != 'CLOSED' AND origin = 'system'` | Implemented |
| SRD-EXE-009.004 | FO-EXE-009 | Must | `on_screener_results(result)` inserts one MONITORING ledger row per passed symbol for today, publishes `SymbolStartedMonitoring` per inserted row, returns the `KeepSet`. | `ScreenerRunResult` | rows + events + `KeepSet` | Idempotent on same-day re-run; "today" uses US/Eastern trading-date | Implemented |
| SRD-EXE-009.005 | FO-EXE-009 | Must | The first system BUY fill against an open MONITORING row atomically flips the earliest such row to ENTERED, sets `positions.anchor_session_date`, and publishes `SymbolEnteredPosition`. | `FillEvent` (system BUY, no open position) | ledger transition + anchor + event | One DB transaction; earliest row = `MIN(session_date)` | Implemented |
| SRD-EXE-009.006 | FO-EXE-009 | Must | A system fill against an already-open position leaves the ledger state unchanged, inserts a `trades` row tagged to the existing anchor, and publishes `SymbolPositionScaled`. | `FillEvent` (non-closing) | trade insert + event | Covers scale-in BUYs and partial SELL fills; closing-fill handled by 009.007 | Implemented |
| SRD-EXE-009.007 | FO-EXE-009 | Must | The system SELL fill that drives `positions.state` to CLOSED flips the anchor row to EXITED and publishes `SymbolExitedPosition`. | `FillEvent` (closing SELL) | ledger transition + event | One DB transaction; missing-anchor edge logs ERROR `[Lifecycle]` without raising | Implemented |
| SRD-EXE-009.008 | FO-EXE-009 | Must | Manual-origin fills are recorded in `trades` only — no ledger read/write, no event. Screener re-emitting an already-held symbol creates a new MONITORING row that never advances to ENTERED. | manual `FillEvent`, repeat screener emit | manual trade row; coexisting ENTERED + MONITORING rows allowed | Re-filtered held symbol is finalised as SKIPPED at next reconcile | Implemented |
| SRD-EXE-009.009 | FO-EXE-009 | Must | ENTERED-state ledger symbols must equal open system positions at every quiescent point; `check_invariant()` reports mismatches. | — | invariant holds; diagnostic available | Asserted in integration tests; violations logged at ERROR `[Lifecycle]` | Implemented |
| SRD-EXE-009.010 | FO-EXE-009 | Must | Expose `MonitoringQuery` (read-only) and `MonitoringCommand` (mutating) as `@runtime_checkable` Protocols from the package's public `__init__.py`. | — | 2 Protocol classes | Consumers type against Protocols only; import-graph test forbids consumer access to `_`-prefixed modules | Implemented |
| SRD-EXE-009.011 | FO-EXE-009 | Must | Expose `MonitoringEventBus` Protocol and a sealed `MonitoringEvent` union of 7 frozen-dataclass event types; `publish` dispatches synchronously and isolates handler exceptions. | — | bus Protocol + 7 event classes | In-process only; handler exceptions logged at ERROR `[Lifecycle]` and do not block siblings | Implemented |
| SRD-EXE-009.012 | FO-EXE-009 | Must | All cross-module DTOs (`KeepSet`, `ReconcileReport`, `FillEvent`, `MonitoringSessionRow`, `InvariantReport`, `ReconcileError`) are `@dataclass(frozen=True, slots=True)` carrying `schema_version: int`. Package surface lives only in `__init__.py`; private modules use `_` prefix; no module under `core/monitoring_session/` imports PyQt6. | — | versioned DTOs + locked layout | CI test enforces Qt-free + private-module boundary | Implemented |

---

## Section 10: Requirements for FO-EXE-010 — Pre-Open Candle DB Reconciliation

| ID | Parent | P | Description | In | Out | Constraints | Status |
|---|---|---|---|---|---|---|---|
| SRD-EXE-010.001 | FO-EXE-010 | Must | `keep_set(today)` returns `filtered = today's passed screener symbols` and `carryover = open system positions`. | `today: date` | `KeepSet` | Read-only, thread-safe; empty `filtered` if no screener run for today | Implemented |
| SRD-EXE-010.002 | FO-EXE-010 | Must | `reconcile_preopen(today)` (1) marks stale MONITORING rows as SKIPPED, (2) computes `evict = stale − keep_set − ENTERED`, (3) per evicted symbol runs one transaction that deletes `price_1m/3m/15m` rows and flips matching ledger rows to EVICTED, (4) publishes `SymbolEvicted` per symbol and `ReconcileCompleted` at end. | `today: date` | `ReconcileReport` + events | One DB transaction per symbol across all three price tables; partial commits forbidden | Implemented |
| SRD-EXE-010.003 | FO-EXE-010 | Must | A symbol is retained if it is in `keep_set.filtered`, in `keep_set.carryover`, or has any ENTERED ledger row. Re-running for the same `today` is a no-op. | retention check, repeat call | retained symbols untouched; idempotent | Invariant violation aborts that symbol's eviction with reason `invariant_violation` | Implemented |
| SRD-EXE-010.004 | FO-EXE-010 | Must | Reconcile fires via `09:15 ET` weekday cron, via startup catch-up when current ET time is in `[09:15, 16:00]` and no run has fired for today, or via direct call; single-flight is enforced by a service lock returning a sentinel report on duplicate calls. | cron / startup / manual | reconcile runs at most once per day per process | Uses `zoneinfo.ZoneInfo("America/New_York")`; inherits existing market-calendar utility | Implemented |
| SRD-EXE-010.005 | FO-EXE-010 | Must | `ReconcileReport` carries `filtered_n`, `carryover_n`, `skipped_n`, `evicted_n`, `evicted_symbols`, `duration_ms`, and `errors`. One INFO summary log per run with topic `[Lifecycle]`; each failed symbol logs WARNING and is retried once after 200 ms before being reported. | reconcile run | populated report + log lines | Transient `OperationalError` triggers retry-once; per-symbol failures isolated | Implemented |
| SRD-EXE-010.006 | FO-EXE-010 | Must | After reconcile, `AppService` feeds `keep_set.filtered ∪ carryover` into `LiveBarWorker.set_symbols` and the next `IntradayCandleLoader`; evicted symbols never reach either consumer; `history(symbol)` returns EVICTED ledger rows post-deletion. | `ReconcileCompleted` event | live feed and loader symbol set; durable history | Handoff goes through `AppService`; workers stay unaware of the service | Implemented |

---

## Section 11: Requirements for FO-EXE-011 — Strategy Engine

| ID | Parent | P | Description | In | Out | Constraints | Status |
|---|---|---|---|---|---|---|---|
| SRD-EXE-011.001 | FO-EXE-011 | Must | `StrategyEngine(QThread)` in `execution/strategy_engine/_engine.py` runs a dedicated `asyncio` loop launched via `asyncio.run()` in `QThread.run()`. Owns the `_StrategyContext` registry, the `ConditionEvaluator`, and the order-queue consumer task. Persisted `run_state` is trusted verbatim on load. | `host` / `port` / IBKR not needed | engine instance | No `PyQt6` imports inside `strategy_engine/` package | Implemented |
| SRD-EXE-011.002 | FO-EXE-011 | Must | `_load_registry()` calls `gui.strategy_builder_dialog.load_strategies()`, instantiates one `_StrategyContext` per record whose `mode != 'disabled'`, and writes `strategy_signal['Status'] = 'Active'` on each context. | startup | `dict[str, _StrategyContext]` keyed by strategy `name` | Disabled records skipped entirely; not loaded into memory | Approved |
| SRD-EXE-011.003 | FO-EXE-011 | Must | `_on_candle_closed(symbol, bar)` is wired to FO-EXE-007's `candle_closed` signal; on each emission it schedules an `asyncio.gather()` of `_evaluate(ctx, symbol, bar)` for every `_StrategyContext` whose symbol-scope filter accepts `symbol`. | `(symbol, OHLCVBar)` | fan-out of per-context coroutines | Coroutines never block the live-bar callback thread | Approved |
| SRD-EXE-011.004 | FO-EXE-011 | Must | `_StrategyContext.accepts(symbol)` returns True for `symbol_mode='all'`, returns `symbol in symbols_include` for `include_only`, and returns `symbol not in symbols_exclude` for `exclude_these`. | `symbol`, context | bool | Filter evaluated before any candle read | Approved |
| SRD-EXE-011.005 | FO-EXE-011 | Must | `_within_schedule(ctx, now_et)` returns True when `now_et.time()` is in `[start_time, end_time)`, `now_et.date()` is in `[start_date, end_date]`, and `now_et.strftime('%A')` is in `ctx.days`. Outside the window the context stays `Inactive` and `_evaluate` returns immediately. | context, current ET datetime | bool | Uses `zoneinfo.ZoneInfo("America/New_York")` | Approved |
| SRD-EXE-011.006 | FO-EXE-011 | Must | `ConditionEvaluator` parses the FO-GUI-013 grammar via tokenizer → recursive-descent AST (parse_or → parse_and → parse_comparison → parse_term) and evaluates against a `dict[str, pd.DataFrame]` of candles keyed by timeframe. The `FUNCTION_MAP` exposes all 14 indicators from the FO-GUI-013 catalogue. | expression string, candles | bool | Indicator function set must equal the FO-GUI-013 catalogue exactly | Approved |
| SRD-EXE-011.007 | FO-EXE-011 | Must | Per-symbol evaluation gate derived from `(ctx.run_state, TradeCycleQuery.has_open_cycle)`. Duplicate signal suppression uses an `in_flight: set[str]` on `_StrategyContext`; `_CycleState` removed (Phase 1). | state, event | gate decision | Mutations guarded by an `asyncio.Lock` per `(strategy_id, symbol)` | Implemented |
| SRD-EXE-011.008 | FO-EXE-011 | Must | `_evaluate(ctx, symbol, bar)` evaluates `entry_condition` only when state = `Active` and `exit_condition` only when state = `Running`. A True result enqueues a `TradeSignal(action, symbol, strategy_id, …)` to a shared `asyncio.Queue` and transitions to `UnderEntry` / `UnderExit`. | context, symbol, bar | `TradeSignal` enqueued | Single shared FIFO queue across all strategies; one consumer task | Approved |
| SRD-EXE-011.009 | FO-EXE-011 | Must | The queue consumer dispatches by `(ctx.mode, ctx.auto_trade)`: `(auto, True)` → `RiskManager.validate()` then `ExecutionRouter.submit()`; `(manual, *)` or `(auto, False)` → `PendingSignalStore.add(signal)`. | dequeued signal | order submitted OR pending row inserted | Auto+True path must reach `ExecutionRouter.submit()` within 50 ms of dequeue | Approved |
| SRD-EXE-011.010 | FO-EXE-011 | Must | A second ENTRY signal for a `(strategy_id, symbol)` pair already in `UnderEntry` / `Running` / `UnderExit` is dropped before enqueue with one DEBUG log `[Strategy] {strategy} {symbol} duplicate ENTRY suppressed`. | second signal | dropped + DEBUG log | No state mutation; no `StrategyEvent` published | Approved |
| SRD-EXE-011.011 | FO-EXE-011 | Must | Before enqueueing an ENTRY signal, `RiskManager.can_allocate(strategy_id, capital_max_pct, signal.entry_price, qty)` is consulted; a False return drops the signal and publishes `StrategySignalDropped(strategy_id, symbol, reason='capital_cap')`. | signal, account state | accept or drop | No state transition on drop; WARNING log with reason | Approved |
| SRD-EXE-011.012 | FO-EXE-011 | Must | A QTimer ticking every 30 s checks all `Running` contexts whose `trade_type == 'Intraday'`. If `now_et >= ctx.end_time`, the engine emits a forced `TradeSignal(EXIT, reason='end_time')` per running symbol and transitions each to `SquareOff`. Positional contexts skip this check. | tick | forced EXIT signals | Within 30 s of crossing `end_time` | Approved |
| SRD-EXE-011.013 | FO-EXE-011 | Must | `emergency_stop()` is synchronous: it enqueues an EXIT signal for every `Running` symbol across every context, sets `_emergency_active = True` blocking further evaluation, and returns only when all those positions have reached `SquareOff`. | external call | all positions exited; engine quiesced | Method must block the caller; reuses the standard order-queue path | Approved |
| SRD-EXE-011.014 | FO-EXE-011 | Must | Every state transition writes back to `strategy_signal` on the in-memory context and persists via `save_strategies(registry)`: `Status`, `Execution_Time`, `Executed_Quantity`, `Pending_Quantity`, `Order_Entry_Status`, `Order_Entry_Timestamp`, `Order_Exit_Status`, `Order_Exit_Timestamp`. | state transition | updated `strategies.json` | Persist debounced to ≥ 250 ms per strategy to avoid disk thrash | Approved |
| SRD-EXE-011.015 | FO-EXE-011 | Must | `StrategyEvent` is a sealed union `StrategyEntered \| StrategyExited \| StrategySquaredOff \| StrategyErrored \| StrategySignalDropped`, each a frozen `@dataclass(slots=True)` with `schema_version: int`. Published on the FO-EXE-009 event bus. | state transition | event published | No engine module imports `PyQt6`; GUI bridges via Qt signals at its own boundary | Approved |
| SRD-EXE-011.016 | FO-EXE-011 | Must | `RexCounterRepository` in `execution/strategy_engine/_rex_counter.py` owns CRUD on a sibling `strategy_rex_counters` table in `candles.db` keyed by `(strategy_id, symbol)`, storing `remaining: int` and `last_updated: TIMESTAMP`. | repository ops | row state | Table created on first use; no FK to `trade_cycles`; reset by `delete(strategy_id)` | Approved |
| SRD-EXE-011.017 | FO-EXE-011 | Must | In `_router.evaluate()` Active branch, after `entry_condition` fires and before the capital-cap check, the engine reads `RexCounterRepository.get(strategy_id, symbol)` and drops the signal when the stored value is `< 0`, publishing `StrategySignalDropped(signal, reason='rex_limit')`. | candle close | accept or drop | Missing row treated as `remaining = cfg.rex_count`; no state mutation on drop | Approved |
| SRD-EXE-011.018 | FO-EXE-011 | Must | `_router.on_order_fill()` entry branch calls `RexCounterRepository.decrement(strategy_id, symbol, init_value=ctx.cfg.rex_count)` immediately after publishing `StrategyEntered`; a missing row is inserted with `remaining = cfg.rex_count - 1`, an existing row decrements `remaining` by 1. | `FillEvent` (entry) | row updated | Idempotent on duplicate fill events for the same `entry_order_id` (see SRD-EXE-012.003) | Approved |
| SRD-EXE-011.019 | FO-EXE-011 | Must | Counter state survives engine restart because rows live in `candles.db`; the repository is queried lazily on each evaluation rather than eagerly loaded into `_StrategyContext`. | engine restart | rows preserved | No in-memory cache between calls — lookup cost is one indexed SELECT per evaluation | Approved |
| SRD-EXE-011.020 | FO-EXE-011 | Must | `TradeSignal` carries a `user_id: int` field populated by `_router._build_entry_signal()` (and every EXIT-signal construction site) from an injected `user_id_provider: Callable[[], int]`. `StrategyEngine.__init__` accepts the provider and forwards it to `_Router`; `AppService` wires it to `lambda: self._active_uid`. Default `user_id=0` indicates "no active user" (engine started without a UI). | candle close / engine init | populated `signal.user_id` | `TradeSignal.schema_version` bumped to `2`; default `qty_recommended` raised from `0` to `1` so manual-mode pending rows render with a non-zero minimum quantity for testing | Approved |
| SRD-EXE-011.021 | FO-EXE-011 | Must | Every EXIT `TradeSignal` sets `qty_recommended` to the held quantity of the matching open cycle (`_open_cycle_qty(strategy_id, symbol)` reads `entry_qty` from `cycle_query.open_cycles_for_strategy`), so a pending exit row and its confirm dialog show the real position size instead of the placeholder default. Covers strategy exits and forced exits (end-time, square-off, emergency). | exit signal construction | `signal.qty_recommended` = open cycle `entry_qty` | Returns 0 when no matching open cycle / no `cycle_query`; supersedes the `qty_recommended=1` placeholder (SRD-EXE-011.020) for exits | Implemented |
| SRD-EXE-011.022 | FO-EXE-011 | Must | Forced EXIT `TradeSignal`s (end-time, square-off, emergency) set `entry_price` to the matching open cycle's last known price (`_open_cycle_price(strategy_id, symbol)` reads `current_price` when it is a positive tick, else `entry_price`, from `cycle_query.open_cycles_for_strategy`), so a paper-mode fill has a realistic reference instead of `None` (which the simulator fills at $0). Strategy exits already carry `bar.close`. | forced exit signal construction | `signal.entry_price` = open cycle last positive price | Reference only — live MARKET orders ignore it; non-positive `current_price` falls back to `entry_price`; returns `None` when no matching open cycle / no `cycle_query` | Implemented |

---

## Section 12: Requirements for FO-EXE-012 — Trade Cycle Ledger

| ID | Parent | P | Description | In | Out | Constraints | Status |
|---|---|---|---|---|---|---|---|
| SRD-EXE-012.001 | FO-EXE-012 | Must | `trade_cycles` SQLAlchemy table (in `db/schema.py`) keys on auto-increment `cycle_id` and carries columns for Identity (`strategy_id`, `symbol`, `user_id`, `monitoring_session_date`), Entry (`entry_time`, `entry_price`, `entry_qty`, `entry_order_id`), Risk snapshot, Live state, Exit, and Outcome groups per FO-EXE-012 §1. | schema bootstrap | new table + index | Index `idx_trade_cycles_state_symbol` on `(state, symbol)`; additive via `create_schema(checkfirst=True)` | Approved |
| SRD-EXE-012.002 | FO-EXE-012 | Must | `TradeCycleService` in `execution/trade_cycle/_service.py` owns CRUD on `trade_cycles`, subscribes to `LiveTickWorker.tick_price` (FO-EXE-008) and FO-EXE-002 fill events, and publishes `TradeCycleEvent` payloads on the FO-EXE-009 event bus. | constructed by `AppService` | service instance | Service is GUI-free; consumers receive Protocol-typed surfaces only | Approved |
| SRD-EXE-012.003 | FO-EXE-012 | Must | `on_entry_fill(fill)` inserts one new `trade_cycles` row with `state='OPEN'`, copies `target_*` and `stoploss_*` from the originating `StrategyConfig` into the row's risk-snapshot columns, links to the broker `positions` row via `entry_order_id`, and publishes `CycleOpened`. | `FillEvent` | new row + `CycleOpened` | Idempotent on `entry_order_id`: duplicate call returns existing row, no new insert | Approved |
| SRD-EXE-012.004 | FO-EXE-012 | Must | Once a row is in `OPENING` / `OPEN` / `CLOSING`, edits to the source `StrategyConfig` are NOT propagated to the row's risk-snapshot columns. Per-cycle changes flow only through `update_risk(cycle_id, …)`. | StrategyConfig edit | no row mutation | `update_risk` is the sole path to mutate `hard_stop_loss`, `target_price`, `trailing_offset`, `trailing_mode` on an open cycle | Approved |
| SRD-EXE-012.005 | FO-EXE-012 | Must | `_on_tick(symbol, price)` for every cycle in `OPEN` / `CLOSING` recomputes `current_price`, `current_pnl_usd = (price − entry_price) × entry_qty`, `current_pnl_pct = (price − entry_price) / entry_price × 100`, `highest_price_seen = max(highest, price)`, trailing level (`$` or `%` mode), and `effective_stop = max(hard_stop_loss, trailing_stop_level)`. Updates persisted on a ≥ 500 ms throttle; a `CycleUpdated` event is published per persist. | `tick_price(symbol, price)` | updated row + `CycleUpdated` | Trailing level only moves up; never below current value | Approved |
| SRD-EXE-012.006 | FO-EXE-012 | Must | After `_on_tick` updates `effective_stop`, if `price ≤ effective_stop` publish `ExitTrigger(cycle_id, reason='hard_sl'` or `'trailing_sl')`; if `price ≥ target_price` publish `ExitTrigger(cycle_id, reason='target')`. Cycle transitions `OPEN` → `CLOSING`. | tick update result | `ExitTrigger` event | Triggers fire regardless of strategy `Mode` or `auto_trade`; consumed by FO-EXE-002 which submits the SELL | Approved |
| SRD-EXE-012.007 | FO-EXE-012 | Must | `on_exit_fill(fill)` transitions the cycle to `CLOSED`, sets `exit_time`, `exit_price`, `exit_qty`, `exit_reason`, freezes `realized_pnl_usd = (exit_price − entry_price) × entry_qty` and `realized_pnl_pct`, publishes `CycleClosed`. | `FillEvent` | row updated + `CycleClosed` | Idempotent on `exit_order_id`: duplicate call returns existing row. Realized quantity is the held `entry_qty` (a cycle only reaches `CLOSED` on a FILLED full-position sell), never the sell fill's reported `exit_qty`. | Implemented |
| SRD-EXE-012.008 | FO-EXE-012 | Must | `on_entry_failed(cycle_id, reason)` transitions an `OPENING` cycle to `ABORTED`, sets `exit_reason` to the reason string, and publishes `CycleAborted`. No tick subscription is attached. | `cycle_id`, reason string | row updated + `CycleAborted` | Only callable while state == `OPENING`; later states raise `InvalidStateTransitionError` | Approved |
| SRD-EXE-012.009 | FO-EXE-012 | Must | Before inserting on `on_entry_fill`, the service shall query for any existing row with the same `(strategy_id, symbol)` in `OPENING` / `OPEN` / `CLOSING` and raise `DuplicateOpenCycleError` if found. | new entry fill | insert or raise | Defence-in-depth: FO-EXE-011 §10 already prevents this at signal emission | Approved |
| SRD-EXE-012.010 | FO-EXE-012 | Must | `TradeCycleQuery` Protocol exposes `open_cycles()`, `cycle(cycle_id)`, `history(symbol=None, strategy_id=None, days=N)`, `has_open_cycle(strategy_id, symbol)`, `open_cycles_for_strategy(strategy_id)`. `CycleSnapshot.state: ExecutionEnums.TradeCycleState` (typed StrEnum, Phase 2). | query call | snapshot tuple | All returns are immutable; thread-safe; no DB locks held across return | Implemented |
| SRD-EXE-012.011 | FO-EXE-012 | Must | `TradeCycleCommand.update_risk(cycle_id, hard_sl=None, target=None, trailing_offset=None, trailing_mode=None)` validates: `hard_sl ≤ current_price`, `target ≥ current_price`, `trailing_offset > 0`. Any failure raises `InvariantViolation` with no partial mutation. Non-terminal-state guard derived from `TradeCycleState.is_non_terminal()`. | edit payload | updated row + `RiskUpdated` | Validation before write; failed call returns unchanged snapshot | Implemented |
| SRD-EXE-012.012 | FO-EXE-012 | Must | `TradeCycleEvent` is a sealed union `CycleOpened \| CycleUpdated \| ExitTrigger \| CycleClosing \| CycleClosed \| CycleAborted \| RiskUpdated`, each a frozen `@dataclass(slots=True)` with `schema_version: int`. Published on the FO-EXE-009 event bus. | state change | event published | No `PyQt6` import in `trade_cycle/` package | Approved |
| SRD-EXE-012.013 | FO-EXE-012 | Must | On startup, `TradeCycleService.reload()` queries every row in `OPENING` / `OPEN` / `CLOSING`, re-attaches each cycle's symbol to the live tick stream via `LiveTickWorker.set_contracts()`, and resumes the update loop. Subsequent reload calls are no-ops (idempotent). | startup or restart | non-terminal rows reattached | Reload must complete before `LiveBarWorker.start()` is called | Approved |

---

## Section 13: Requirements for FO-EXE-013 — Strategy Run Lifecycle

| ID | Parent | P | Description | In | Out | Constraints | Status |
|---|---|---|---|---|---|---|---|
| SRD-EXE-013.001 | FO-EXE-013 | Must | ▶ Play sets `run_state = RUNNING` and persists to registry; state survives restart. | user ▶ Play action | updated registry | Persist within 250 ms of action | Implemented |
| SRD-EXE-013.002 | FO-EXE-013 | Must | ■ Stop with no open cycles sets `run_state = STOPPED` immediately. | user ■ Stop, no open cycles | updated registry | Synchronous transition | Implemented |
| SRD-EXE-013.003 | FO-EXE-013 | Must | ■ Stop with open cycles sets `run_state = SQUARING_OFF`; engine emits forced EXIT per open cycle; auto-transitions to `STOPPED` when last cycle is terminal. | user ■ Stop, open cycles present | EXIT signals; eventual `STOPPED` | No new ENTRY signals during `SQUARING_OFF` | Implemented |
| SRD-EXE-013.004 | FO-EXE-013 | Must | `STOPPED` blocks all `TradeSignal` emission regardless of candle-close or tick events. | candle close or tick | no signal emitted | Short-circuit before any indicator evaluation | Implemented |
| SRD-EXE-013.005 | FO-EXE-013 | Must | `RUNNING` with no open cycle for `(strategy_id, symbol)` evaluates entry condition only. | candle close, `has_open_cycle = False` | `TradeSignal(ENTRY)` if true | Derived from `TradeCycleLedger.has_open_cycle()` | Implemented |
| SRD-EXE-013.006 | FO-EXE-013 | Must | `RUNNING` with an open cycle in `TradeCycleState.OPEN` evaluates exit condition only. | candle close, `has_open_cycle = True` | `TradeSignal(EXIT)` if true | No entry evaluation while cycle is open | Implemented |
| SRD-EXE-013.007 | FO-EXE-013 | Must | `SQUARING_OFF` emits only forced EXIT signals; no ENTRY signals. | candle close during `SQUARING_OFF` | forced EXIT signals only | Guard applied before entry-condition evaluation | Implemented |
| SRD-EXE-013.008 | FO-EXE-013 | Must | On registry load, map legacy `Status` values: `"Inactive"` → `STOPPED`; `"Active"` or `"Running"` → `RUNNING`; remove old `Status` key. | registry file load | `run_state` field populated | Idempotent: if `run_state` already present, skip | Implemented |

---

## Section 14: Requirements for FO-EXE-014 — Broker Order State Machine

| ID | Parent | P | Description | In | Out | Constraints | Status |
|---|---|---|---|---|---|---|---|
| SRD-EXE-014.001 | FO-EXE-014 | Must | `trades` table carries `order_state TEXT NOT NULL DEFAULT 'NEW'` and `filled_quantity INTEGER NOT NULL DEFAULT 0`. The legacy `status` and `pnl` columns are dropped. `positions.state` is dropped. | `db/schema.py` migration | new columns; old columns removed | Migration is idempotent via PRAGMA-guarded ALTER; backfill maps `status='SUBMITTED'→'NEW'`, `'FILLED'→'FILLED'`, `'CLOSED'→'FILLED'` | Implemented |
| SRD-EXE-014.002 | FO-EXE-014 | Must | `TradeRecord` dataclass exposes `order_state: BuyOrderState \| SellOrderState` and `filled_quantity: int`; the `status` and `pnl` fields are removed. Pylance / mypy `--strict` typing enforced via `Union[ExecutionEnums.BuyOrderState, ExecutionEnums.SellOrderState]`. | `data/models.py` | typed dataclass | StrEnum auto-serialises to the column string value on insert | Implemented |
| SRD-EXE-014.003 | FO-EXE-014 | Must | `ExecutionEngine.submit_order()` writes the initial `trades` row with `order_state=NEW` and `filled_quantity=0` for the order's side (`BuyOrderState.NEW` for BUY, `SellOrderState.NEW` for SELL). | submitted order | new `trades` row | Insert must complete before broker order ID is returned to the caller | Implemented |
| SRD-EXE-014.004 | FO-EXE-014 | Must | `ExecutionEngine.handle_order_fill(fill_event)` updates the matching `trades` row: `filled_quantity += fill.quantity`; `order_state = FILLED` when `filled_quantity == total_quantity` else `PARTIAL_FILLED`. The `positions` row is updated per SRD-EXE-005.002 / .003. | broker fill event | updated `trades` + `positions` | One DB transaction per fill; idempotent on duplicate `(broker_order_id, fill_seq)` | Implemented |
| SRD-EXE-014.005 | FO-EXE-014 | Must | Broker rejection is handled in `OrderIngestion.on_order_event` (broker-agnostic, FO-EXE-015): it sets `order_state = REJECTED` on the `trades` row, then for an entry order calls `TradeCycleCommand.abort_entry_order(entry_order_id, 'broker_reject')`, which aborts the owning OPENING cycle to ABORTED and is a no-op when no cycle was opened. | broker reject event | `trades.order_state='REJECTED'`; `CycleAborted` when an OPENING cycle exists | `filled_quantity` reflects shares filled before the reject (0 if rejected before any fill) | Implemented |
| SRD-EXE-014.006 | FO-EXE-014 | Must | Broker cancel routed through `ExecutionEngine.handle_order_cancel(cancel_event)` sets `order_state = CANCELLED` on the `trades` row; `filled_quantity` is preserved at whatever the broker reported before the cancel; the owning cycle stays in its current state. | broker cancel event | `trades.order_state='CANCELLED'` | A SELL cancelled after partial fill keeps the cycle OPEN with `positions.quantity = entry_qty - filled` | Implemented |
| SRD-EXE-014.007 | FO-EXE-014 | Must | `TradeCycleService.on_entry_fill` transitions OPENING → OPEN only when the BUY `trades.order_state` reaches `BuyOrderState.FILLED`; PARTIAL_FILLED holds the cycle in OPENING. | fill event sequence | OPEN cycle only at fully filled BUY | Strict mode per Final_Execution.md §5.3.3 default — eager open is out of scope | Implemented |
| SRD-EXE-014.008 | FO-EXE-014 | Must | `core/monitoring_session/_service.on_fill(side, order_state, qty)` consumes `(side, order_state)` to drive `LifecycleState` transitions: first `BuyOrderState.FILLED` (with `qty > 0`) flips a MONITORING row to ENTERED; `SellOrderState.FILLED` that drives `positions.quantity → 0` flips ENTERED to EXITED. | fill event from EXE | LifecycleState transition | Single entry point; no other module mutates LifecycleState | Implemented |

---

## Section 15: Requirements for FO-EXE-015 — Broker-Agnostic Order Ingestion & Adapter

| ID | Parent | P | Description | In | Out | Constraints | Status |
|---|---|---|---|---|---|---|---|
| SRD-EXE-015.001 | FO-EXE-015 | Must | `BrokerAdapter` (`execution/broker_adapter.py`) translates a `TradeSignal` into a broker `OrderRequest` (signal_id → `client_ref`), calls `Broker.place_order`, and returns the broker order id to the router. Wired into `app_service` as the `submitter`, replacing `PaperBroker`. | `TradeSignal`, qty | `OrderRequest` + broker order id | Adapter is the only execution component importing a concrete broker; preserves `ExecutionSubmitter.submit` signature for the router | Implemented |
| SRD-EXE-015.002 | FO-EXE-015 | Must | On order acceptance the ingestion pipeline inserts one `trades` row with `trade_id = broker_order_id`, `order_state = NEW`, `filled_quantity = 0`, for both paper and live orders. | accepted order | new `trades` row | Paper orders are no longer bypassed; insert completes before fill events are processed | Implemented |
| SRD-EXE-015.003 | FO-EXE-015 | Must | On each inbound `OrderEvent` the pipeline calls `update_trade_fill` (advancing `order_state` + `filled_quantity`), feeds `strategy_engine.on_order_fill(FillEvent)`, and advances the owning `trade_cycles` row via `TradeCycleCommand`. | `OrderEvent` | updated `trades` + `trade_cycles` | One broker-agnostic handler; no branch on broker type. `trade_cycles` is the single live-position surface — the legacy `positions` table is written only by unwired legacy code and is retired in Phase 6 (no positions-table write here) | Implemented |
| SRD-EXE-015.004 | FO-EXE-015 | Must | `build_broker(mode, broker_name)` (`execution/broker_factory.py`) routes paper → `SimBroker`, live → name-keyed registry (`IBKR` → `IBKRBroker`). Wired into `app_service`. Forwards an optional `price_provider` to `SimBroker` for live-priced paper fills (cross-tool: SRD-INF-009.007). | `(mode, broker_name)` | bound broker + event subscription | Routing + registry done; `app_service` passes hard-coded `paper`/`IBKR` — reading the values from `users.mode`/Settings is the deferred GUI step | Partial |
| SRD-EXE-015.005 | FO-EXE-015 | Must | The pipeline maps broker `OrderStatus` onto `BuyOrderState` (BUY) / `SellOrderState` (SELL) by order side, relying on identical member values — no lookup table. | `OrderEvent.status`, side | `trades.order_state` value | Mapping must fail loudly (raise) if a status value has no matching execution-enum member | Implemented |
| SRD-EXE-015.006 | FO-EXE-015 | Must | Once the adapter and ingestion are verified live, `ExecutionEngine`, `PaperEngine`, `PaperBroker`, and the paper-only `app_service` methods (`_on_paper_fill`, `_record_paper_entry`, `_record_paper_exit`) are removed. | legacy modules | deleted code | Existing `trades`/`trade_cycles` audit rows retained; removal gated on contract tests passing | Implemented |

---

## Section 16: Requirements for FO-EXE-016 — Retire `positions` Table; OrderIngestion-Driven Monitoring Lifecycle

| ID | Parent | P | Description | In | Out | Constraints | Status |
|---|---|---|---|---|---|---|---|
| SRD-EXE-016.001 | FO-EXE-016 | Must | On a completed entry fill, `OrderIngestion` advances the symbol's `monitoring_session` ledger row `MONITORING → ENTERED` via `transition_to_entered`. | completed entry `OrderEvent` | ledger row `ENTERED` | Only a fully-filled entry transitions; a partial entry leaves the row `MONITORING` (SRD-EXE-014.008) | Implemented |
| SRD-EXE-016.002 | FO-EXE-016 | Must | On a completed exit fill, `OrderIngestion` advances the same ledger row `ENTERED → EXITED` via `transition_to_exited`. | completed exit `OrderEvent` | ledger row `EXITED` | Fires only when the owning `trade_cycle` reaches `CLOSED` | Implemented |
| SRD-EXE-016.003 | FO-EXE-016 | Must | `open_system_position_symbols` returns the symbols of all non-terminal `trade_cycles`, reading no `positions` data. | none | `frozenset[str]` of open symbols | Queries `trade_cycles` by table name (`state NOT IN CLOSED/ABORTED`); no execution-schema import inside `core/` | Implemented |
| SRD-EXE-016.004 | FO-EXE-016 | Must | The monitoring repository drops `upsert_position_with_anchor`, `has_open_system_position`, and `position_anchor`, and removes the `positions` import; ledger-transition methods are retained. | none | slimmer `MonitoringRepository` | `transition_to_entered`/`transition_to_exited` and `insert_trade_with_anchor` behaviour preserved as needed by the seam | Implemented |
| SRD-EXE-016.005 | FO-EXE-016 | Must | The unwired `MonitoringCommand.on_fill` hook is removed; its lifecycle role is served by the ingestion seam (016.001/.002). | none | `on_fill` deleted; dead tests removed | `MONITORING` creation via `on_screener_results` is unchanged | Implemented |
| SRD-EXE-016.006 | FO-EXE-016 | Must | The `positions` `sa.Table`, the `DatabaseManager` methods `upsert_position`/`delete_position`/`fetch_open_positions`, and the `positions` lifecycle-migration column entries are removed. | schema + manager | dropped table + methods | Existing `trades`/`trade_cycles` rows retained; done as an isolated migration commit | Implemented |

---

## Section 17: Requirements for FO-EXE-017 — Absolute Capital Allocation, Capital-Max Sizing & Advisory Risk Warnings

> Refines: SRD-EXE-001.002 (sizing now from strategy Capital Max, not risk-per-trade), SRD-EXE-005.004 (capital basis is absolute `effective_capital`, not `equity × max_allocation_pct`), SRD-EXE-011.011 (`can_allocate` basis), SRD-EXE-011.017 (rex gate unchanged; reset trigger added).
> Cross-tool (not EXE IDs): GUI Settings "Max capital" field `%`→`$` and advisory pop-up — see `gui/SRD.md`. `RiskConfig` field migration `max_allocation_pct`→`max_capital_value` — see `infrastructure/SRD.md`.

| ID | Parent | P | Description | In | Out | Constraints | Status |
|---|---|---|---|---|---|---|---|
| SRD-EXE-017.001 | FO-EXE-017 | Must | An `effective_capital_provider() -> float` resolves the per-user trading budget: paper mode returns `max_capital_value`; live mode returns `max_capital_value` when `≤ account.total_cash_value`, else `0.9 × total_cash_value`. | active user `RiskConfig`, `AccountState`, mode | `float` budget | Stored `max_capital_value` is never mutated; live reconcile runs once per connect | Implemented |
| SRD-EXE-017.002 | FO-EXE-017 | Must | When live `max_capital_value > total_cash_value`, emit one LIVE-LOG WARNING `[Risk] Max Capital ($%s) exceeds broker cash ($%s) — using $%s` and use the 90%-of-cash budget. | live reconcile | WARNING log | Single emission per connect; no pop-up required for this case | Implemented |
| SRD-EXE-017.003 | FO-EXE-017 | Must | `RiskManager.size_for_strategy(entry_price, capital_max_pct, effective_capital) -> int` returns `floor((effective_capital × capital_max_pct / 100) / entry_price)`; the resulting `entry_price × qty ≤ strategy_budget`. | entry price, `capital_max_pct`, budget | `int` shares | `entry_price ≤ 0` returns 0; always `floor()`; supersedes fixed `qty_recommended=1` in `_build_entry_signal` | Implemented |
| SRD-EXE-017.004 | FO-EXE-017 | Must | When `size_for_strategy(...) < 1`, the router drops the ENTRY (no order, no state change), publishes `StrategySignalDropped(signal, reason='capital_insufficient')`, and emits LIVE-LOG WARNING `[Strategy] %s Capital Max insufficient for entry on %s`. | sized qty `< 1` | dropped signal + WARNING | Blocking control; evaluated before order submission | Implemented |
| SRD-EXE-017.005 | FO-EXE-017 | Must | `RiskManager.can_allocate(strategy_id, capital_max_pct)` computes its limit from `effective_capital × capital_max_pct / 100` (not `account.equity`); a strategy whose open-position value plus this entry would breach the limit is dropped (blocking). | strategy id, `capital_max_pct`, budget | `CanAllocateResult` | Replaces the `account.equity`-based limit in `risk_manager.py:62` | Implemented |
| SRD-EXE-017.006 | FO-EXE-017 | Must | Max Position, Risk per trade, and Max Daily Loss are advisory: breaches emit a WARNING log and a GUI pop-up via the event bus, and never block, resize, or close an order. `RiskManager.validate_signal` no longer rejects on these checks. | signal, account, config | advisory `RiskWarning` event(s) | `ValidationResult.ok` stays True for these breaches; only circuit breaker + capital cap block | Implemented |
| SRD-EXE-017.007 | FO-EXE-017 | Must | A daily-loss check sums combined unrealised + realised loss across all currently active trades for the user; when the total `≤ -max_daily_loss_value` it emits one advisory `RiskWarning(kind='daily_loss')` per breach crossing. | active cycles, day PnL, `max_daily_loss` | advisory `RiskWarning` | Aggregate, not per-position; debounced to one warning per crossing, not per tick | Implemented |
| SRD-EXE-017.008 | FO-EXE-017 | Must | `app_service` constructs `RiskManager(config, account_provider, cb_state_provider, user_id, tracker)` and passes it as the engine `risk=`, replacing `PassthroughRiskValidator()`. | engine wiring | live `RiskManager` | `tracker.get_all(user_id)` sourced from open `trade_cycles`; preserves `RiskValidator` protocol | Implemented |
| SRD-EXE-017.009 | FO-EXE-017 | Must | `_build_entry_signal` sets `qty_recommended = size_for_strategy(...)` for the owning strategy instead of the literal `1`. | context, symbol, bar | sized `TradeSignal` | Single source of sizing; pending/manual signals carry the same sized qty | Implemented |
| SRD-EXE-017.010 | FO-EXE-017 | Must | `_apply_run_state` calls `RexCounterRepository.reset(strategy_id)` on a `STOPPED → RUNNING` transition so each per-stock re-entry budget returns to the configured `rex_count`. | run-state transition | rex rows cleared | Reset only on start transition; pause/stop does not reset; manual reset action retained | Implemented |
| SRD-EXE-017.011 | FO-EXE-017 | Should | The Active Trades Rex column renders **remaining re-entries**: an exhausted counter shows `0` (not a negative number), and a pending signal for a symbol does not display another row's `(strategy, symbol)` counter. | cycle/pending row | display string | Display-only; stored `remaining` value is unchanged (gate still blocks at `< 0`); refines SRD-GUI-014.013 (now Reopen) | Implemented |
| SRD-EXE-017.012 | FO-EXE-017 | Must | (GUI surface) The Settings → User "Max capital" control changes from a `%` `QDoubleSpinBox` to a `$` `QDoubleSpinBox` and reads/writes `RiskConfig.max_capital_value`. | user edit | persisted `max_capital_value` | Implemented in `gui/settings_panel.py`; range/step suited to dollars; no `%` suffix | Implemented |
| SRD-EXE-017.013 | FO-EXE-017 | Must | (GUI surface) Advisory `RiskWarning` events (017.006/.007) are bridged to a non-blocking GUI pop-up plus the Live Log; the pop-up never gates an order. | `RiskWarning` event | pop-up + Live Log line | Bridged at the GUI boundary off the event bus; debounced to avoid pop-up storms | Implemented |
| SRD-EXE-017.014 | FO-EXE-017 | Must | (INF/data surface) `RiskConfig.max_allocation_pct` (%) is replaced by `max_capital_value: float` ($) across `data/models.py`, `config/settings.py`, `gui/user_store.py`, `user/manager.py`; legacy settings-JSON keys are migrated on load. | existing settings JSON | migrated `RiskConfig` | Old `max_allocation_pct` key read once and mapped/ dropped; default `max_capital_value` chosen sensibly (e.g. `2000.0`) | Implemented |
| SRD-EXE-017.015 | FO-EXE-017 | Must | `RiskManager.margin_available() -> float` returns `effective_capital − sum(average_price × quantity)` over every open cycle of the user across all strategies. | tracker open cycles, `effective_capital` | `float` remaining budget | Derived live each call, never stored; floored at 0.0; supersedes the unused `can_enter_new` | Approved |
| SRD-EXE-017.016 | FO-EXE-017 | Must | The router blocks an ENTRY whose `entry_price × qty > margin_available()` (checked after `can_allocate`), publishing `StrategySignalDropped(signal, reason='margin_exhausted')` and emitting one edge-triggered LIVE-LOG WARNING. | sized entry signal, margin | dropped signal + WARNING | Blocking global ceiling spanning all strategies; warns once per crossing like the capital-cap path | Approved |
| SRD-EXE-017.017 | FO-EXE-017 | Must | `margin_available()` also subtracts the value of in-flight (submitted-not-filled) and pending entry signals so that multiple entries firing on the same bar cannot collectively exceed Max Capital. | in-flight + pending signals | reserved margin | Reservation cleared on fill, reject, or rollback; prevents same-bar over-commit | Approved |
| SRD-EXE-017.018 | FO-EXE-017 | Must | Entry sizing clamps to remaining margin: `qty = floor(min(strategy_slice, margin_available()) / entry_price)`, so a single entry never exceeds what is actually left even when the strategy slice is larger. | strategy slice, margin, price | clamped `int` shares | Refines SRD-EXE-017.003/.009; `qty < 1` still drops with `capital_insufficient` | Approved |
| SRD-EXE-017.019 | FO-EXE-017 | Must | `get_account_state` sets `open_position_value` by summing `entry_price × qty` over the user's open cycles instead of the hardcoded `0.0`, so paper-mode margin and the GUI capital indicator are correct. | open cycles | populated `AccountState` | Paper and disconnected modes only; live IBKR value is left untouched | Approved |
| SRD-EXE-017.020 | FO-EXE-017 | Should | After each live fill the system compares `margin_available()` against broker cash and emits one advisory `RiskWarning(kind='margin_drift')` when they differ beyond a tolerance. | live fill, broker cash | advisory `RiskWarning` | Live mode only (paper exempt); debounced to one warning per crossing, never blocks | Approved |
| SRD-EXE-017.021 | FO-EXE-017 | Must | (GUI surface) The User View `_AdminContextBar` shows Max Capital and Margin Available, refreshed on `account_updated`; green when margin `> 0`, red otherwise. | `account_updated` signal | context-bar cells | Display-only; reads `effective_capital()` and `margin_available()`; mirror GUI row added to `gui/SRD.md` | Approved |
